<UserControl x:Class="Labb3_NET22.View.ChooseQuizUserControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Labb3_NET22.View"
        mc:Ignorable="d"
        Height="450" Width="800">
    
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>

        </Grid.ColumnDefinitions>

        <Grid.Background>
            <ImageBrush ImageSource="/Images/QuizBackground.jpg"
                        Stretch="UniformToFill"/>
        </Grid.Background>

        <Border Grid.Row="2" Grid.Column="2" 
            Background="#AA000000" 
            Padding="10" 
            CornerRadius="10">

            <TextBlock Text="Choose Quiz" 
                       FontSize="24"
                       Foreground="GhostWhite"/>
        </Border>

        <Border Grid.Row="3" Grid.Column="2">
            
            <ListBox x:Name="QuizListBox"
                     DisplayMemberPath="Title"
                     MouseDoubleClick="QuizListBox_MouseDoubleClick"
                     Background="DarkOliveGreen"/>

        </Border>

        <Button Grid.Row="5"
                Grid.Column="2"
                Content="Go Back"
                Background="DarkOliveGreen"
                Click="GoBack_Click"/>


    </Grid>
</UserControl>
