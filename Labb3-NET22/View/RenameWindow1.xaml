<Window x:Class="Labb3_NET22.View.RenameWindow1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Labb3_NET22.View"
        mc:Ignorable="d"
        Title="RenameWindow1" Height="100" Width="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        

        <Border Grid.Row="1" Grid.Column="1">
            <TextBox Text="Enter new title..." 
                     x:Name="NewTitle" 
                     Foreground="Gray"
                     GotFocus="GotFocus"
                     LostFocus="LostFocus"/>
        </Border>

        <Border Grid.Row="2" Grid.Column="1">
            <Button x:Name="SaveNewTitleBtn" 
                    Content="Save" 
                    Click="SaveNewTitleClick"/>
        </Border>
    </Grid>

</Window>
